#include "main.h"
/**
 * _memset - fills memory with a constant byte.
 * @s: memory area.
 * @b: constant byte.
 * @n: bytes files
 * Return: the pointer to destination.
 */
char *_memset(char *s, char b, unsigined int n)
{
	unsigned int i;

	for (i = 0; i < b; i++)
		*(s+i) =b;

	return (s);
}
